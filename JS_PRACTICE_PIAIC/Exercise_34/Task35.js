// Task 35: 
// Animals.

let Animals = ["Cat", "Dog", "lion"];

Animals.map((Animal)=>{
    if (Animal === "Cat") {
        console.log(`${Animal} is a pet.`);
    }
    else if (Animal === "Dog") {
        console.log(`${Animal} is a loyal friend.`);
    }
    else{
        console.log(`${Animal} is brave`);
    }
})

console.log("ALL of these are carnivors.\nLoves to eat meat and bones and also an intelligent");






